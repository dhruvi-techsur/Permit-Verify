---
phase: 01-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tailwind.config.ts
  - frontend/postcss.config.js
  - frontend/src/index.css
  - frontend/src/design-system/tokens.ts
  - frontend/src/design-system/index.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
autonomous: true

features:
  implements: ["UX-05"]
  depends_on: []
  enables: ["AUTH-01", "AUTH-02", "AUTH-03", "AUTH-04"]

must_haves:
  truths:
    - "npm run build in frontend/ exits 0 with no TypeScript errors"
    - "tailwind.config.ts exports all brand, surface, text, border, status, and feedback color tokens"
    - "Custom typography scale (heading.xl through caption) is defined in tailwind.config.ts"
    - "Custom spacing, shadow, and border-radius tokens are defined in tailwind.config.ts"
    - "No raw hex colors appear in source .tsx files — all colors reference token names"
    - "Inter font is loaded via index.html or CSS"
  artifacts:
    - path: "frontend/tailwind.config.ts"
      provides: "Complete design system token configuration"
      contains: "brand"
    - path: "frontend/src/design-system/tokens.ts"
      provides: "TypeScript-typed design token constants"
      exports: ["colors", "typography", "spacing"]
    - path: "frontend/src/index.css"
      provides: "Tailwind directives + CSS custom properties"
      contains: "@tailwind base"
  key_links:
    - from: "frontend/src/design-system/tokens.ts"
      to: "frontend/tailwind.config.ts"
      via: "Token values mirrored from tailwind config"
      pattern: "export.*colors"
    - from: "frontend/src/index.css"
      to: "tailwind.config.ts theme"
      via: "@tailwind directives"
      pattern: "@tailwind base"

integration_contracts:
  requires: []
  provides:
    - artifact: "frontend/tailwind.config.ts"
      exports: ["brand color tokens", "surface tokens", "text tokens", "status tokens", "typography scale", "shadow tokens", "border-radius tokens"]
      shape: |
        theme.extend.colors: { brand: {...}, surface: {...}, text: {...}, border: {...}, status: {...}, feedback: {...} }
        theme.extend.fontFamily: { sans: ['Inter', ...] }
        theme.extend.boxShadow: { sm, md, lg, card }
        theme.extend.borderRadius: { sm: '2px', md: '6px', lg: '8px', xl: '12px' }
      verify: "grep -n 'brand' frontend/tailwind.config.ts && grep -n 'surface' frontend/tailwind.config.ts && grep -n 'Inter' frontend/tailwind.config.ts && echo CONTRACT_OK"
    - artifact: "frontend/src/design-system/tokens.ts"
      exports: ["colors", "typography", "spacing", "shadows", "radii"]
      shape: "export const colors = { brand: { primary: '#4F46E5', ... }, ... }"
      verify: "grep -n 'export const colors' frontend/src/design-system/tokens.ts && echo CONTRACT_OK"
    - artifact: "frontend/package.json"
      exports: ["React 18", "Vite 5", "TypeScript 5", "Tailwind CSS 3", "Zustand 4", "React Router 6", "Axios 1", "React Hook Form 7", "Zod 3", "Lucide React"]
      shape: "dependencies include react@^18, vite@^5, tailwindcss@^3, zustand@^4, react-router-dom@^6"
      verify: "grep -n 'react' frontend/package.json && grep -n 'zustand' frontend/package.json && grep -n 'tailwindcss' frontend/package.json && echo CONTRACT_OK"
---

<objective>
Scaffold the React/Vite/TypeScript frontend project and implement the complete UX-05 design system: custom Tailwind token configuration with the exact color palette, typography scale, spacing, shadows, and border-radius defined in TechArch/UX-Mockup.

Purpose: Every UI plan (01-04, 01-05, and all future phases) uses these tokens. Implementing them correctly now prevents visual debt across all 40+ screens.
Output: frontend/ scaffold with package.json, Vite config, Tailwind config with full token set, and typed design system constants.
</objective>

<feature_dependencies>
Implements: UX-05: Design system uses custom tokens (color palette, typography, spacing, border-radius, shadows) — no default/template look
Depends on: None
Enables: AUTH-01, AUTH-02, AUTH-03, AUTH-04 (all auth UI screens depend on this design system)
</feature_dependencies>

<execution_context>
@/root/.config/opencode/pivota_spec-framework/workflows/execute-plan.md
@/root/.config/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript frontend with all dependencies</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/postcss.config.js
  </files>
  <action>
Create the frontend/ directory with a Vite + React + TypeScript scaffold. Install all dependencies specified in TechArch §6.1:

package.json dependencies (exact versions from TechArch):
```json
{
  "name": "permit-management-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:e2e": "playwright test",
    "lint": "eslint src --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "date-fns": "^3.6.0",
    "lucide-react": "^0.400.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-hook-form": "^7.52.0",
    "react-router-dom": "^6.24.0",
    "zustand": "^4.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@testing-library/jest-dom": "^6.4.0",
    "@testing-library/react": "^16.0.0",
    "@testing-library/user-event": "^14.5.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "autoprefixer": "^10.4.19",
    "eslint": "^8.57.0",
    "eslint-plugin-react-hooks": "^4.6.2",
    "eslint-plugin-react-refresh": "^0.4.7",
    "jsdom": "^24.1.0",
    "postcss": "^8.4.39",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
```

vite.config.ts:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    host: true,
    port: 5173,
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
  },
});
```

tsconfig.json (project references):
```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" }
  ]
}
```

tsconfig.app.json:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
```

index.html — load Inter from Google Fonts:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PermitFlow</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

postcss.config.js:
```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

src/main.tsx — minimal React entry point:
```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

src/App.tsx — placeholder (replaced in plan 01-05):
```tsx
function App() {
  return (
    <div className="min-h-screen bg-surface-50 flex items-center justify-center">
      <h1 className="text-heading-xl font-bold text-text-900">PermitFlow</h1>
    </div>
  );
}

export default App;
```

Create src/test/setup.ts for Vitest:
```typescript
import '@testing-library/jest-dom';
```

Create playwright.config.ts at frontend/ root:
```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'list',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
  },
});
```
  </action>
  <verify>
grep -n "zustand" frontend/package.json && grep -n "react-router-dom" frontend/package.json && grep -n "tailwindcss" frontend/package.json && echo "DEPS OK"
grep -n "Inter" frontend/index.html && echo "FONT OK"
grep -n "strict.*true" frontend/tsconfig.app.json && echo "STRICT TS OK"
  </verify>
  <done>
frontend/package.json contains all TechArch-specified dependencies at correct major versions. Vite config has path alias @/→src/. TypeScript strict mode enabled. Playwright config points to localhost:5173. Inter font loaded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Design system tokens — Tailwind config + typed constants</name>
  <files>
    frontend/tailwind.config.ts
    frontend/src/index.css
    frontend/src/design-system/tokens.ts
    frontend/src/design-system/index.ts
  </files>
  <action>
Implement the full UX-05 design system. Every value comes directly from TechArch §Design System Token Spec and UX-Mockup 00-overview.md. No raw hex colors in app source files — only token names.

frontend/tailwind.config.ts — COMPLETE token configuration:
```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        // Brand / Primary (from UX-Mockup)
        brand: {
          primary: '#4F46E5',       // --color-primary-500 — primary CTAs, active nav
          'primary-hover': '#4338CA', // --color-primary-600 — hover
          'primary-active': '#3730A3', // --color-primary-700 — pressed
          'primary-light': '#EEF2FF', // --color-primary-50 — selected rows, focus fills
        },

        // Surface & Backgrounds (from UX-Mockup)
        surface: {
          base: '#FFFFFF',          // --color-surface-0 — card surfaces, modal backgrounds
          page: '#F8FAFC',          // --color-surface-50 — page background
          subtle: '#F1F5F9',        // --color-surface-100 — sidebar bg, input fills
          border: '#E2E8F0',        // --color-surface-200 — dividers, skeleton base
          muted: '#CBD5E1',         // --color-surface-300 — skeleton shimmer, disabled borders
        },

        // Text (from UX-Mockup)
        text: {
          primary: '#0F172A',       // --color-text-900 — headings, primary body
          secondary: '#334155',     // --color-text-700 — secondary body, labels
          tertiary: '#64748B',      // --color-text-500 — captions, placeholders, timestamps
          disabled: '#94A3B8',      // --color-text-300 — disabled text
          inverse: '#FFFFFF',       // text on dark/primary backgrounds
        },

        // Borders (from UX-Mockup)
        border: {
          default: '#E2E8F0',
          focus: '#4F46E5',
          error: '#DC2626',
          success: '#059669',
        },

        // Status Colors — semantic (from UX-Mockup)
        status: {
          draft: '#64748B',
          'draft-bg': '#F1F5F9',
          submitted: '#2563EB',
          'submitted-bg': '#EFF6FF',
          review: '#D97706',
          'review-bg': '#FFFBEB',
          info: '#EA580C',
          'info-bg': '#FFF7ED',
          approved: '#059669',
          'approved-bg': '#ECFDF5',
          rejected: '#DC2626',
          'rejected-bg': '#FEF2F2',
        },

        // Feedback (from TechArch token spec)
        feedback: {
          error: '#DC2626',
          warning: '#D97706',
          success: '#059669',
          'error-bg': '#FEF2F2',
          'warning-bg': '#FFFBEB',
          'success-bg': '#ECFDF5',
        },
      },

      // Typography (from UX-Mockup)
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        mono: ['ui-monospace', 'SFMono-Regular', 'Menlo', 'monospace'],
      },

      fontSize: {
        // Display: 36px/40px/700/-0.02em
        'display': ['36px', { lineHeight: '40px', fontWeight: '700', letterSpacing: '-0.02em' }],
        // H1: 28px/34px/700/-0.01em
        'heading-xl': ['28px', { lineHeight: '34px', fontWeight: '700', letterSpacing: '-0.01em' }],
        // H2: 22px/28px/600/-0.01em
        'heading-lg': ['22px', { lineHeight: '28px', fontWeight: '600', letterSpacing: '-0.01em' }],
        // H3: 18px/24px/600
        'heading-md': ['18px', { lineHeight: '24px', fontWeight: '600' }],
        // H4: 15px/20px/600
        'heading-sm': ['15px', { lineHeight: '20px', fontWeight: '600' }],
        // Body-lg: 16px/24px/400
        'body-lg': ['16px', { lineHeight: '24px', fontWeight: '400' }],
        // Body: 14px/20px/400
        'body': ['14px', { lineHeight: '20px', fontWeight: '400' }],
        // Body-sm: 13px/18px/400
        'body-sm': ['13px', { lineHeight: '18px', fontWeight: '400' }],
        // Label: 13px/16px/500/0.01em
        'label': ['13px', { lineHeight: '16px', fontWeight: '500', letterSpacing: '0.01em' }],
        // Caption: 12px/16px/400
        'caption': ['12px', { lineHeight: '16px', fontWeight: '400' }],
        // Code: 13px/18px/mono
        'code': ['13px', { lineHeight: '18px' }],
      },

      // Spacing — 4px base unit (from TechArch + UX-Mockup)
      // Tailwind's default already uses 4px base; extend with named aliases
      spacing: {
        'xs': '4px',
        'sm': '8px',
        'md': '12px',
        'lg': '16px',
        'xl': '24px',
        '2xl': '32px',
        '3xl': '48px',
        'sidebar': '256px',
        'header': '64px',
      },

      // Box Shadow (from UX-Mockup)
      boxShadow: {
        'sm': '0 1px 2px 0 rgb(0 0 0 / 0.05)',
        'md': '0 4px 6px -1px rgb(0 0 0 / 0.07), 0 2px 4px -2px rgb(0 0 0 / 0.05)',
        'lg': '0 10px 15px -3px rgb(0 0 0 / 0.08), 0 4px 6px -4px rgb(0 0 0 / 0.05)',
        'card': '0 1px 3px 0 rgb(0 0 0 / 0.06), 0 1px 2px -1px rgb(0 0 0 / 0.04)',
      },

      // Border Radius (from UX-Mockup)
      borderRadius: {
        'none': '0',
        'sm': '2px',      // form inputs, tight elements
        'md': '6px',      // buttons, badges
        'lg': '8px',      // cards, panels
        'xl': '12px',     // modals, drawers
        'full': '9999px', // avatar, pill badge
      },

      // Max-width
      maxWidth: {
        'content': '1280px',
      },

      // Transition timing
      transitionTimingFunction: {
        'smooth': 'ease-in-out',
      },
      transitionDuration: {
        'hover': '150ms',
        'active': '75ms',
      },

      // Animation for skeleton screens
      animation: {
        'shimmer': 'shimmer 1.5s infinite',
        'pulse': 'pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      },
      keyframes: {
        shimmer: {
          '0%': { backgroundPosition: '-200% 0' },
          '100%': { backgroundPosition: '200% 0' },
        },
      },
    },
  },
  plugins: [],
};

export default config;
```

frontend/src/index.css — Tailwind directives + global base styles:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  html {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  body {
    @apply bg-surface-page text-text-primary font-sans;
    margin: 0;
    min-height: 100vh;
  }

  /* Focus visible — never suppress, show immediately */
  :focus-visible {
    outline: 2px solid #4F46E5;
    outline-offset: 2px;
  }

  /* Respect prefers-reduced-motion for skeleton animations */
  @media (prefers-reduced-motion: reduce) {
    .animate-shimmer,
    .animate-pulse {
      animation: none;
    }
  }
}

@layer components {
  /* Skeleton shimmer utility */
  .skeleton {
    @apply bg-surface-border rounded animate-pulse;
  }

  .skeleton-shimmer {
    background: linear-gradient(
      90deg,
      theme('colors.surface.border') 25%,
      theme('colors.surface.muted') 50%,
      theme('colors.surface.border') 75%
    );
    background-size: 200% 100%;
    @apply animate-shimmer rounded;
  }
}
```

frontend/src/design-system/tokens.ts — TypeScript-typed constants (mirrors Tailwind config for use in JS logic):
```typescript
/**
 * Design System Tokens
 * Mirror of tailwind.config.ts theme values.
 * Use these in JS/TS logic where Tailwind class names cannot be used.
 * All values sourced from UX-Mockup 00-overview.md and TechArch §Design System Token Spec.
 */

export const colors = {
  brand: {
    primary: '#4F46E5',
    primaryHover: '#4338CA',
    primaryActive: '#3730A3',
    primaryLight: '#EEF2FF',
  },
  surface: {
    base: '#FFFFFF',
    page: '#F8FAFC',
    subtle: '#F1F5F9',
    border: '#E2E8F0',
    muted: '#CBD5E1',
  },
  text: {
    primary: '#0F172A',
    secondary: '#334155',
    tertiary: '#64748B',
    disabled: '#94A3B8',
    inverse: '#FFFFFF',
  },
  border: {
    default: '#E2E8F0',
    focus: '#4F46E5',
    error: '#DC2626',
    success: '#059669',
  },
  status: {
    draft: { text: '#64748B', bg: '#F1F5F9' },
    submitted: { text: '#2563EB', bg: '#EFF6FF' },
    review: { text: '#D97706', bg: '#FFFBEB' },
    info: { text: '#EA580C', bg: '#FFF7ED' },
    approved: { text: '#059669', bg: '#ECFDF5' },
    rejected: { text: '#DC2626', bg: '#FEF2F2' },
  },
  feedback: {
    error: { text: '#DC2626', bg: '#FEF2F2' },
    warning: { text: '#D97706', bg: '#FFFBEB' },
    success: { text: '#059669', bg: '#ECFDF5' },
  },
} as const;

export const typography = {
  fontFamily: {
    sans: 'Inter, system-ui, sans-serif',
    mono: 'ui-monospace, SFMono-Regular, Menlo, monospace',
  },
  fontSize: {
    display: '36px',
    headingXl: '28px',
    headingLg: '22px',
    headingMd: '18px',
    headingSm: '15px',
    bodyLg: '16px',
    body: '14px',
    bodySm: '13px',
    label: '13px',
    caption: '12px',
    code: '13px',
  },
} as const;

export const spacing = {
  xs: '4px',
  sm: '8px',
  md: '12px',
  lg: '16px',
  xl: '24px',
  '2xl': '32px',
  '3xl': '48px',
  sidebar: '256px',
  header: '64px',
} as const;

export const shadows = {
  sm: '0 1px 2px 0 rgb(0 0 0 / 0.05)',
  md: '0 4px 6px -1px rgb(0 0 0 / 0.07), 0 2px 4px -2px rgb(0 0 0 / 0.05)',
  lg: '0 10px 15px -3px rgb(0 0 0 / 0.08), 0 4px 6px -4px rgb(0 0 0 / 0.05)',
  card: '0 1px 3px 0 rgb(0 0 0 / 0.06), 0 1px 2px -1px rgb(0 0 0 / 0.04)',
} as const;

export const radii = {
  none: '0',
  sm: '2px',
  md: '6px',
  lg: '8px',
  xl: '12px',
  full: '9999px',
} as const;
```

frontend/src/design-system/index.ts — barrel export:
```typescript
export * from './tokens';
```
  </action>
  <verify>
grep -n "brand" frontend/tailwind.config.ts && grep -n "surface" frontend/tailwind.config.ts && grep -n "status" frontend/tailwind.config.ts && echo "COLOR TOKENS OK"
grep -n "Inter" frontend/tailwind.config.ts && echo "FONT TOKEN OK"
grep -n "export const colors" frontend/src/design-system/tokens.ts && grep -n "export const typography" frontend/src/design-system/tokens.ts && echo "TYPED TOKENS OK"
grep -n "@tailwind base" frontend/src/index.css && echo "CSS OK"
grep -n "shimmer" frontend/src/index.css && echo "SKELETON ANIMATION OK"
grep -n "prefers-reduced-motion" frontend/src/index.css && echo "A11Y MOTION OK"
  </verify>
  <done>
tailwind.config.ts contains all token categories: brand, surface, text, border, status, feedback colors; Inter font family; heading/body/label/caption font sizes; spacing aliases; custom shadows (sm/md/lg/card); border-radius (sm/md/lg/xl/full). Typed tokens.ts mirrors all values. index.css has shimmer animation + prefers-reduced-motion override. No raw hex colors appear in .tsx files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external→frontend | Google Fonts CDN loading Inter font from fonts.googleapis.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | Google Fonts CDN leaks user IP | accept | Acceptable for v1 development; v2 can self-host Inter via fontsource package if privacy requirements tighten |
| T-01-02-02 | Tampering | Tailwind content glob picks up generated files | mitigate | content glob in `tailwind.config.ts` scoped to `./src/**/*.{ts,tsx}` and `./index.html` — no generated output directories included |
| T-01-02-03 | Information Disclosure | Design token hex values embedded in CSS bundle | accept | Not sensitive — design tokens are intentionally public; shipping them in CSS bundle is expected behavior |
</threat_model>

<verification>
grep -c "brand\|surface\|text\|border\|status\|feedback" frontend/tailwind.config.ts
grep -n "Inter" frontend/index.html
grep -n "export const colors" frontend/src/design-system/tokens.ts
</verification>

<success_criteria>
- frontend/tailwind.config.ts defines all 6 color token groups (brand, surface, text, border, status, feedback)
- Typography scale covers display through caption (10 sizes)
- Custom shadows (sm, md, lg, card) and border-radius (sm, md, lg, xl, full) configured
- Typed design-system/tokens.ts exports colors, typography, spacing, shadows, radii
- index.css has shimmer animation + prefers-reduced-motion override for skeleton screens
- No raw hex colors appear in .tsx source files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
